
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	char a[101];
	int str;
	cin.getline(a,sizeof(a));
	str = strlen(a);

	cout <<str;




	return 0;
}
